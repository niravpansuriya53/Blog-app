name: Django-Blog-App

on:
  workflow_call:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: MacOS-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Massage 
        run: echo  Hello word